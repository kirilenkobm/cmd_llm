# Command line interface for Deepseek LLM.

Quick and dirty.
Written exclusively with Cursor.

Create a .env file in the same directory as main.py with the following:

SK_TOKEN=<YOUR DEEPSEEK API KEY>

Then link with something like

```bash
ln -s <path to this repo>/main.py <Some $PATH directory>/llm
```
